{
  "name": "00-public",
  "comment": "Unprotected pass-through for login page and static assets. Must be first so session checks on route 02 do not cause redirect loops.",
  "condition": "${find(request.uri.path, '^/$') or find(request.uri.path, '^/PrimaryAuth') or find(request.uri.path, '^/static/') or find(request.uri.path, '^/assets/') or find(request.uri.path, '^/favicon') or find(request.uri.path, '^/manifest') or find(request.uri.path, '^/callback')}",
  "baseURI": "http://cbonline.lloyds.com:3000",
  "heap": [],
  "handler": "ClientHandler"
}